If data is unavailable from any subagent, state it is unavailable{{ " and leave the corresponding field null" if null_fields else "" }}. NEVER estimate, approximate, or fabricate numbers.{{ " NEVER conflate the target company with its peers." if no_conflation else "" }} Always use dedicated financial tools for standard calculations — do not re-implement formulas in reasoning text or in sandbox code. Use the `execute` tool (sandbox) only for ad-hoc computations not covered by your financial tools.
